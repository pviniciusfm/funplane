apiVersion: apiextensions.k8s.io/v1beta1
kind: CustomResourceDefinition
metadata:
  name: gateway.network.fanplane.io
spec:
  group: network.fanplane.io
  version: v1
  names:
    kind: Gateway
    plural: gateways
  scope: Namespaced
  validation:
    openAPIV3Schema:
      properties:
        spec:
          properties:
            replicas:
              type: integer
              minimum: 1
              maximum: 10
